# 🤖 Настройка Telegram бота и Mini App

## Часть 1: Создание бота

### Шаг 1: Создайте бота через BotFather

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите название бота (например: "Натуральные Соки")
4. Введите username бота (должен заканчиваться на "bot", например: `naturaljuices_bot`)
5. **Сохраните токен!** Он выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

**Пример диалога:**
```
Вы: /newbot
BotFather: Alright, a new bot. How are we going to call it?
Вы: Натуральные Соки
BotFather: Good. Now let's choose a username for your bot.
Вы: naturaljuices_bot
BotFather: Done! Congratulations on your new bot.
```

### Шаг 2: Настройте описание и аватар

```
/setdescription
Выберите бота → Введите описание
```

**Пример описания:**
```
🍊 Свежие натуральные соки с доставкой!

Мы предлагаем:
• 100% натуральные соки
• Свежие фрукты и овощи
• Быстрая доставка
• Доступные цены

Нажмите "Открыть меню" чтобы сделать заказ!
```

```
/setabouttext
Выберите бота → Введите краткое описание
```

**Пример:**
```
Натуральные соки с доставкой 🍹
```

```
/setuserpic
Выберите бота → Загрузите фото (512x512px)
```

### Шаг 3: Настройте команды бота

```
/setcommands
Выберите бота → Введите список команд
```

**Скопируйте и вставьте:**
```
start - 🚀 Начать работу
admin - 🔐 Панель администратора
help - ❓ Помощь
```

## Часть 2: Создание Mini App

### Шаг 1: Создайте Web App

1. Отправьте команду `/newapp` в BotFather
2. Выберите вашего бота
3. Введите название Mini App (например: "Заказ соков")
4. Введите описание:
   ```
   Выберите любимые соки и оформите заказ с доставкой
   ```
5. **Загрузите фото** (640x360px):
   - Можно создать в Canva или Figma
   - Должно быть привлекательным и показывать соки
6. **Загрузите GIF** (опционально, для демонстрации)
7. Введите **URL вашего приложения**:
   - Для разработки: `https://localhost:3000` (не будет работать в реальном Telegram!)
   - Для production: `https://your-app.vercel.app`

**⚠️ Важно:** URL должен быть по **HTTPS**! HTTP не работает в Telegram Mini Apps.

### Шаг 2: Получите Short Name

После создания Web App BotFather даст вам **Short Name**.

Теперь ваш Mini App доступен по ссылке:
```
https://t.me/your_bot_username/short_name
```

## Часть 3: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Supabase (из предыдущего шага)
VITE_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Telegram Bot
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_MINI_APP_URL=https://t.me/your_bot/your_app

# Ваш Telegram ID (для админ доступа)
ADMIN_TELEGRAM_IDS=123456789
```

### Как узнать свой Telegram ID:

1. Откройте [@userinfobot](https://t.me/userinfobot)
2. Нажмите Start
3. Бот пришлет ваш ID (например: `123456789`)
4. Скопируйте это число в `ADMIN_TELEGRAM_IDS`

**Несколько админов:**
```env
ADMIN_TELEGRAM_IDS=123456789,987654321,555666777
```

## Часть 4: Деплой приложения

### Вариант 1: Vercel (Рекомендуется для frontend)

1. Создайте аккаунт на [vercel.com](https://vercel.com)
2. Подключите ваш GitHub репозиторий
3. Vercel автоматически определит Vite проект
4. Добавьте переменные окружения:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
5. Нажмите Deploy
6. После деплоя скопируйте URL (например: `https://juice-app.vercel.app`)

**Через CLI:**
```bash
npm install -g vercel
vercel login
vercel
# Следуйте инструкциям
```

### Вариант 2: Netlify

```bash
npm install -g netlify-cli
netlify login
netlify deploy --prod
```

### Обновление URL в BotFather

После деплоя вернитесь в BotFather:

```
/editapp
Выберите бота → Выберите Web App
Edit Web App URL
Введите новый URL: https://juice-app.vercel.app
```

## Часть 5: Деплой бота

### Вариант 1: Railway (Рекомендуется)

1. Создайте аккаунт на [railway.app](https://railway.app)
2. Нажмите "New Project" → "Deploy from GitHub repo"
3. Выберите ваш репозиторий
4. Railway автоматически определит Node.js проект
5. Перейдите в Settings → Variables
6. Добавьте все переменные из `.env`:
   ```
   VITE_SUPABASE_URL=...
   VITE_SUPABASE_ANON_KEY=...
   TELEGRAM_BOT_TOKEN=...
   TELEGRAM_MINI_APP_URL=...
   ADMIN_TELEGRAM_IDS=...
   ```
7. В Settings → Deploy настройте:
   - **Start Command**: `npm run bot`
8. Нажмите Deploy

**Бот запустится автоматически!**

### Вариант 2: Render

1. Создайте аккаунт на [render.com](https://render.com)
2. New → Background Worker
3. Подключите GitHub репозиторий
4. Настройте:
   - **Build Command**: `npm install`
   - **Start Command**: `npm run bot`
5. Добавьте переменные окружения
6. Create Web Service

### Вариант 3: VPS (Ubuntu)

```bash
# На сервере
git clone your-repo
cd juice-telegram-app
npm install

# Установите PM2
npm install -g pm2

# Создайте .env файл
nano .env
# Вставьте переменные окружения

# Запустите бота с PM2
pm2 start npm --name "juice-bot" -- run bot
pm2 save
pm2 startup
```

## Часть 6: Проверка работы

### Тестирование бота:

1. Откройте вашего бота в Telegram
2. Нажмите Start
3. Вы должны увидеть:
   - Приветственное сообщение
   - Кнопку "🍹 Открыть меню"
   - Кнопки "📋 Мои заказы" и "📞 Контакты"

### Тестирование Mini App:

1. Нажмите "🍹 Открыть меню"
2. Должно открыться приложение с каталогом соков
3. Попробуйте добавить сок в корзину
4. Оформите тестовый заказ

### Тестирование админ-панели:

1. Отправьте команду `/admin` боту
2. Если вы добавили свой ID в `ADMIN_TELEGRAM_IDS`, вы увидите меню администратора
3. Попробуйте посмотреть статистику

## Часть 7: Настройка уведомлений (опционально)

Для получения уведомлений о новых заказах в Telegram можно настроить webhook:

```javascript
// В bot/index.js добавьте:
supabase
  .channel('orders')
  .on('postgres_changes', 
    { event: 'INSERT', schema: 'public', table: 'orders' },
    async (payload) => {
      const order = payload.new
      
      // Отправляем уведомление админу
      ADMIN_IDS.forEach(adminId => {
        bot.telegram.sendMessage(
          adminId,
          `🔔 Новый заказ!\n\n` +
          `📦 Номер: ${order.order_number}\n` +
          `💰 Сумма: ${order.total_amount} сум\n` +
          `📞 Телефон: ${order.phone_number}`
        )
      })
    }
  )
  .subscribe()
```

## Типичные проблемы и решения

### ❌ "Bot token is invalid"
**Решение**: Проверьте токен в `.env`. Убедитесь, что нет лишних пробелов.

### ❌ Mini App не открывается
**Решение**: 
- Проверьте URL в BotFather (должен быть HTTPS)
- Убедитесь, что приложение задеплоено и доступно
- Проверьте консоль браузера (F12) на ошибки

### ❌ "Failed to fetch"
**Решение**: Проверьте переменные Supabase в `.env`

### ❌ Кнопка "Открыть меню" не появляется
**Решение**: Проверьте команду `/start` в коде бота. Убедитесь, что используется `Markup.button.webApp()`

### ❌ Админ команды не работают
**Решение**: Проверьте свой Telegram ID в `ADMIN_TELEGRAM_IDS`

## Полезные команды для отладки

### Проверка работы бота локально:
```bash
npm run bot
```

В консоли должно появиться:
```
🤖 Bot started successfully
```

### Просмотр логов бота на Railway:
```bash
railway logs
```

### Просмотр логов бота на PM2:
```bash
pm2 logs juice-bot
```

## Улучшения для production

1. **Добавьте обработку ошибок**:
   ```javascript
   process.on('unhandledRejection', (error) => {
     console.error('Unhandled rejection:', error)
   })
   ```

2. **Добавьте логирование**:
   ```bash
   npm install winston
   ```

3. **Настройте мониторинг**:
   - Используйте Sentry для отслеживания ошибок
   - Настройте алерты в Railway/Render

4. **Включите rate limiting**:
   ```javascript
   import rateLimit from 'telegraf-ratelimit'
   
   bot.use(rateLimit({
     window: 1000,
     limit: 3
   }))
   ```

## Готово! 🎉

Ваш бот и Mini App полностью настроены и готовы к использованию!

**Следующие шаги:**
1. Протестируйте все функции
2. Добавьте реальные данные о соках
3. Настройте способы оплаты (если нужно)
4. Добавьте свою контактную информацию
5. Начните принимать заказы! 🍊
